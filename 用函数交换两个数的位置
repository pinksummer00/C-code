#include<stdio.h>
void swap(int* pa, int* pb)
{
	int tmp = 0;
	tmp = *pa;
	*pa = *pb;
	*pb = tmp;
}
int main()
{
	//int a = 10;
	//int* pa = &a; //pa是指针变量
	//*pa = 20;//解引用操作
	int a = 10;
	int b = 20;
	printf("a=%d b=%d\n", a, b);
	swap(&a, &b);
	printf("交换顺序后的a=%d b=%d\n", a, b);
	return 0;
}
