#include<stdio.h>
int count_bit_one(int n)
{
	int count = 0;
	while (n)
	{
		n = n & (n - 1);//按位与，都是1才是1
		count++;
	}
	return count;
}
int main()
{
	int a = 0;
	scanf("%d", &a);//写一个函数求a的二进制位中有几个1
	int count = count_bit_one(a);
	printf("count=%d\n", count);
	return 0;
}
