#include <stdio.h>
#include<stddef.h>
struct s
{
	char c;
	int i;
	double d;
};
int main()
{
	printf("%d\n", offsetof(struct s,c));//头文件是<stddef.h>
	printf("%d\n", offsetof(struct s,i));
	printf("%d\n", offsetof(struct s,d));
	return 0;
}
