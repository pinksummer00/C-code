#include<stdio.h>
void Add(int* p)
{
	(*p)++; //调用函数，使得num每次增加1
}
int main()
{
	int num = 0;
	Add(&num);
	printf("num=%d\n", num);
	Add(&num);
	printf("num=%d\n", num);
	Add(&num);
	printf("num=%d\n", num);
	return 0;
}
