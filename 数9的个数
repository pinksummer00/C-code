#include<stdio.h>
int main()
{
	int i = 0;
	int count = 0;
	for (i = 1; i <= 100; i++)
	{
		if (i % 10 == 9) //个位是9
			count++;
		if (i / 10 == 9) //十位是9
			count++;
	}
	printf("count=%d\n", count); //1-100之间数9的个数
	return 0;
}
