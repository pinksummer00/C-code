#include<stdio.h>
struct Stu //创建了一个结构体类型Stu
{
	char name[20];
	int age;
	char id[20];
};
int main()
{
	int a = 10;
	struct Stu s1 = { "张三",20,"2021010305" };//使用结构体类型创建了一个学生对象s1，并初始化
/*printf("%s\n", s1.name);*/
	struct Stu* ps = &s1;
	printf("%s\n", (*ps).name);
	return 0;
}

